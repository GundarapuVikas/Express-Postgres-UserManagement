const host="localhost";
const user="postgres";
const password="240102";
const name="test";
const dialect="postgres";
const max_pool=5;
const min_pool=0;
const acquire_pool=30000;
const idle_pool=10000;

module.exports={
    host,
    user,
    password,
    name,
    dialect,
    max_pool,
    min_pool,
    acquire_pool,
    idle_pool
}
